# Generated by Chef for <%= node['fqdn'] %>
# Local modifications will be overridden

<% if @user_install %>
if [ -f "${HOME}/.nvm/nvm.sh" ]; then
  . "${HOME}/.nvm/nvm.sh"
fi
<% else %>
if [ -f "/usr/local/src/nvm/nvm.sh" ]; then
  . "/usr/local/src/nvm/nvm.sh"
fi
<% end %>
